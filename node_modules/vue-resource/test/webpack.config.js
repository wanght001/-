var webpack = require('webpack');

module.exports = {
    entry: __dirname + '/index.jstest',
    output: {
        path: __dirname + '/',
        filename: 'specs.jstest'
    },
    module: {
        loaders: [
            {test: /\.js$/, loader: 'buble-loader', exclude: /node_modules/}
        ]
    },
    plugins: [
        new webpack.optimize.ModuleConcatenationPlugin()
    ]
};
